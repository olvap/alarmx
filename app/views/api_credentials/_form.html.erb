<%= form_with(model: api_credential) do |form| %>
  <% if api_credential.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(api_credential.errors.count, "error") %> prohibited this api_credential from being saved:</h2>

      <ul>
        <% api_credential.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
