<!DOCTYPE html>
<html>
  <head>
     <meta charset="UTF-8">
    <title>AlarmX</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <!-- Compiled and minified JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/@materializecss/materialize@1.2.2/dist/js/materialize.min.js"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>


  <body class="<%= controller_name %>">
    <!-- <p style="color: green"><%= notice %></p> -->

    <% if current_user %>
      <nav>
        <div class="nav-wrapper blue-grey darken-3">
          <a href="#" class="brand-logo center">Admin Panel</a>
          <ul id="nav-mobile" class="left hide-on-med-and-down">
            <li><%= link_to 'Api Credentials', admin_api_credentials_path %></li>
            <li><%= link_to 'Sensors', admin_sensors_path %></li>
            <li><%= link_to 'Users', admin_users_path %></li>
          </ul>
          <ul class="right hide-on-med-and-down">
            <li><%= link_to 'Logout', destroy_user_session_path, method: :get %></li>
          </ul>
        </div>
      </nav>

    <% else %>

    <% end %>

    <div class="container">
    <%= yield %>
    </div>
  </body>
</html>
