<h1>Editing emitter</h1>

<%= render "form", emitter: @emitter, building: @building %>

<br>

<div>
  <%= link_to "Show this emitter", building_emitter_path(@building, @emitter) %> |
  <%= link_to "Back to emitters", building_emitters_path(@building) %>
</div>
